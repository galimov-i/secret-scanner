# Сканер Секретов Конфигурации

Надежный CLI инструмент для сканирования директорий с целью обнаружения захардкоженных секретов и чувствительной информации в вашей кодовой базе.

## Возможности

- 🔍 **Рекурсивное сканирование директорий**: Автоматически сканирует все файлы в текущей директории и поддиректориях
- 🎯 **Множественные паттерны секретов**: Обнаруживает AWS ключи, приватные ключи, API токены, URL баз данных и многое другое
- ⚠️ **Классификация уровня риска**: Категоризирует секреты как ВЫСОКИЙ, СРЕДНИЙ или НИЗКИЙ риск
- 🎨 **Богатый терминальный интерфейс**: Красивые прогресс-бары и таблицы с цветовой кодировкой используя библиотеку `rich`
- 🚫 **Умная фильтрация**: Автоматически игнорирует распространенные директории (`.git`, `venv`, `node_modules`, и т.д.) и бинарные файлы
- 📊 **Детальные отчеты**: Показывает путь к файлу, номер строки, тип секрета, уровень риска и замаскированный фрагмент
- 🔒 **Безопасность**: Маскирует чувствительную информацию в выводе для безопасного просмотра
- ⚡ **Производительность**: Читает файлы построчно для эффективной работы с большими кодовыми базами

## Требования

- Python 3.10 или выше
- Библиотека `rich` (устанавливается автоматически через скрипт установки)

## Установка

### Быстрая установка (Рекомендуется)

Запустите скрипт установки:

```bash
chmod +x install.sh
./install.sh
```

Этот скрипт:
- Проверит версию Python (требуется 3.10+)
- Установит необходимые зависимости (`rich`)
- Сделает сканер исполняемым

### Ручная установка

1. Установите зависимости Python:
```bash
pip3 install -r requirements.txt
```

2. Сделайте сканер исполняемым (опционально):
```bash
chmod +x scanner.py
```

## Использование

Сканирование текущей директории:

```bash
python3 scanner.py
```

Или, если вы сделали его исполняемым:

```bash
./scanner.py
```

Сканер будет:
1. Рекурсивно сканировать все файлы в текущей директории
2. Показывать прогресс-бар со статусом сканирования
3. Показывать сводку с разбивкой по уровням риска
4. Отображать детальную таблицу со всеми обнаруженными секретами

## Обнаруживаемые типы секретов

### ВЫСОКИЙ РИСК
- AWS Access Key ID (например, `AKIA...`)
- AWS Secret Access Keys
- Приватные ключи (RSA, DSA, EC, OpenSSH)
- URL баз данных со встроенными паролями (PostgreSQL, MySQL, MongoDB)

### СРЕДНИЙ РИСК
- Общие API токены и токены доступа
- GitHub Personal Access Tokens (`ghp_...`)
- Slack Webhook URL
- Stripe Live Keys
- Slack токены

### НИЗКИЙ РИСК
- Общие переменные пароля (`password = "..."`)
- Переменные секретов (`secret = "..."`)
- Переменные API ключей (`apikey = "..."`)

## Игнорируемые пути

Сканер автоматически игнорирует:

**Директории:**
- `.git`, `__pycache__`, `venv`, `env`
- `.idea`, `.vscode`, `node_modules`
- `.pytest_cache`, `.mypy_cache`

**Расширения файлов:**
- Изображения: `.png`, `.jpg`, `.jpeg`, `.gif`, `.bmp`, `.ico`
- Бинарные файлы: `.pyc`, `.pyo`, `.exe`, `.dll`, `.so`, `.dylib`
- Архивы: `.zip`, `.tar`, `.gz`
- Шрифты: `.woff`, `.woff2`, `.ttf`, `.eot`
- Документы: `.pdf`

## Обработка ошибок

Сканер корректно обрабатывает:
- **Ошибки прав доступа**: Пропускает файлы без прав на чтение
- **Ошибки Unicode**: Использует `errors='ignore'` для обработки бинарных файлов
- **Большие файлы**: Обрабатывает файлы построчно для минимизации использования памяти

## Пример вывода

```
Config Secrets Scanner

Scanning directory: /path/to/project

[cyan]Scanning files...[/cyan] ████████████████████ 100% 0:00:02

📊 Summary
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Total Secrets Found: 5

HIGH Risk:   3
MEDIUM Risk: 1
LOW Risk:    1
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔍 Secret Scan Results
┏━━━━━━━━━━━━━━━━━━━━┳━━━━━┳━━━━━━━━━━━━━━━━━━━━┳━━━━━━━━━━┳━━━━━━━━━━━━━┓
┃ File Path          ┃ Line┃ Secret Type        ┃ Risk     ┃ Snippet     ┃
┡━━━━━━━━━━━━━━━━━━━━╇━━━━━╇━━━━━━━━━━━━━━━━━━━━╇━━━━━━━━━━╇━━━━━━━━━━━━━┩
│ _test_secrets.env  │   5 │ AWS Access Key ID  │ HIGH     │ AKIA****    │
│ _test_secrets.env  │   6 │ AWS Secret...      │ HIGH     │ wJal****    │
│ _test_secrets.env  │  16 │ Database URL...    │ HIGH     │ postgres... │
│ _test_secrets.env  │  20 │ GitHub Personal... │ MEDIUM   │ ghp_****    │
│ _test_secrets.env  │  27 │ Password Variable  │ LOW      │ test****    │
└────────────────────┴─────┴────────────────────┴──────────┴─────────────┘

⚠️  Secrets detected! Please review and remove them immediately.
```

## Тестирование

Включен тестовый файл `_test_secrets.env` с фейковыми секретами для демонстрации. Запустите сканер, чтобы увидеть его в действии!

**Примечание:** Этот файл содержит фейковые секреты только для тестирования. Удалите его перед сканированием вашей реальной кодовой базы.

## Ограничения

- **Построчное сканирование**: Многострочные секреты (например, полные приватные ключи) могут быть не полностью обнаружены при разрывах строк
- **Ложные срабатывания**: Некоторые паттерны могут вызывать ложные срабатывания в тестовых файлах или документации
- **Покрытие паттернами**: Дополнительные типы секретов могут потребовать пользовательских паттернов для конкретных случаев использования

## Примечание по безопасности

⚠️ **ВАЖНО**: Этот инструмент предназначен для обнаружения секретов, которых НЕ должно быть в вашей кодовой базе. Если вы найдете реальные секреты:

1. **Немедленно смените/отзовите** скомпрометированные учетные данные
2. **Удалите** секреты из вашей кодовой базы
3. **Используйте переменные окружения** или безопасные решения для управления секретами
4. **Проверьте историю git**, чтобы убедиться, что секреты не были закоммичены ранее

## Лицензия

Этот проект предоставляется "как есть" для целей сканирования безопасности.

## Вклад в проект

Не стесняйтесь отправлять issues или pull requests для улучшения обнаружения паттернов или добавления новых функций.
